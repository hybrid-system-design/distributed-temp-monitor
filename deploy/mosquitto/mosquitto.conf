# Mosquitto config for the temperature-monitor stack.
# Anonymous access on the LAN / demo network is intentional for this scale;
# add authentication or TLS before exposing the broker beyond a trusted network.
listener 1883
allow_anonymous true
persistence false
log_dest stdout
